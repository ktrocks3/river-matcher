# GeoMatcher: Exact Finite-Domain Matching of Embedded River Networks

GeoMatcher computes exact finite-domain matchings between embedded river networks. The public application name is GeoMatcher; the internal Python package remains `river_matcher`.

## Command Line

```powershell
geomatcher SOURCE_GRAPH.txt TARGET_GRAPH.txt --cost relative_length_error
```
